# Entity-intelligence POC — Colab/runtime dependencies
numpy
pandas
pyarrow
faiss-cpu
python-igraph
datasketch
google-genai
gradio
# orchestration (notebook 09 executes 01-08)
nbformat
nbconvert
ipykernel

# --- Layer 1-4 architecture ---
rapidfuzz                 # fuzzy matching for entity resolution
# Optional production backends. Both are adapters behind one-method interfaces
# (TokenNERBackend / CorefResolver) and activate automatically when installed;
# without them the deterministic offline backends are used.
# gliner                  # zero-shot token-level NER (Layer 1 safety net)
# fastcoref               # neural coreference resolution
