# Entity-intelligence POC — Colab/runtime dependencies
numpy
pandas
pyarrow
faiss-cpu
python-igraph
datasketch
google-genai
gradio
# orchestration (notebook 09 executes 01-08)
nbformat
nbconvert
ipykernel

# --- Layer 1-4 architecture ---
rapidfuzz                 # fuzzy matching for entity resolution
splink                    # Layer 2: Fellegi-Sunter entity resolution (src/entity_resolution.py)
duckdb                    # Splink's default backend
# --- Production backends ---
# These are NOT optional any more and do NOT activate "automatically". A
# missing backend raises rather than falling through to a corpus-fitted
# stand-in, because a silent fallback produces output shaped exactly like a
# real run and quietly invalidates every number measured from it.
# gliner: Layer 1 token-level NER. NER_BACKEND="gliner" raises
# NERBackendUnavailable if it cannot load.
gliner
# fastcoref: neural coreference; rule-based resolver used otherwise.
fastcoref

# Research/offline backends live in src/research/ and must be selected by
# name (NER_BACKEND=deterministic, GENAI_MODE=offline). Nothing selects them
# for you.
